# Governance Foundation Docusaurus Site

This folder contains the canonical Governance Foundation site.

Use this app for all active site and content work.

## Current scope

- `docs/knowledgefund/*`
- `docs/governance/*`
- `docs/knowledgeontology/*`
- `docs/frameworks/*`
- `blog/*`
- homepage in `src/pages/index.tsx`

## Notes

- Docusaurus is the active site and content path.
- Assets are mirrored under `static/assets` for rendering.
- Mermaid is enabled via `@docusaurus/theme-mermaid` and `markdown.mermaid`.
- Deploy validation note (2026-04-18): this README line was updated to force a fresh deploy run from `develop` after CI change-detection fixes.
